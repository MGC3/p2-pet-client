{{> ChangePassword}}
{{> SignOut}}
<div class="home__container">
  <button class="btn btn-primary pet-form__btn">Go to new pet form</button>

  {{#each pets as |pet|}}
  <h1>Pet name is: {{pet.name}}</h1>
  <h1>Pet ID is: {{pet.id}}</h1>
  <button class='btn btn-primary pet-show__btn'>Go to my {{pet.name}}'s page</button>
  {{/each}}
</div>